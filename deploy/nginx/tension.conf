server {
    listen 80;
    server_name _;  # Reemplazar con tu dominio o IP del VPS

    client_max_body_size 2M;

    # Archivos estáticos
    location /static/ {
        alias /home/antonio/Tension/staticfiles/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # Proxy a Gunicorn
    location / {
        proxy_pass         http://127.0.0.1:8000;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120;   # Mayor que --timeout de Gunicorn (90s) por Claude API
        proxy_connect_timeout 10;
    }
}

# Para producción con HTTPS (descomenta tras configurar certbot):
# server {
#     listen 443 ssl;
#     server_name tudominio.com;
#
#     ssl_certificate     /etc/letsencrypt/live/tudominio.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tudominio.com/privkey.pem;
#
#     # ... mismos bloques location que arriba ...
# }
#
# server {
#     listen 80;
#     server_name tudominio.com;
#     return 301 https://$host$request_uri;
# }
